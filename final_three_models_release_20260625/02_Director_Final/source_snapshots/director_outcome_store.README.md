# Director Outcome Store

The original `director_outcome_store.db` snapshot is intentionally not stored inside this final release folder.

Reason:

- it is a very large SQLite artifact
- it is not needed for code handoff
- keeping it here creates confusion between executable package contents and heavy local archives

Authoritative local path:

`/Users/tongyin/Desktop/InsightBridge_九大模型_v2026/reports/director_outcome_store.db`
