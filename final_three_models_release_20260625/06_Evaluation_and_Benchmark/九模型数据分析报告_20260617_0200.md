# 九模型数据分析报告

生成时间：2026-06-17 02:00

## 一、当前状态

- 三大系统的最近一轮结果已经完整写入：
  - `System 2` 最新小时：`H177`
  - `System 3` 最新小时：`H177`
  - `System 1` 最新结果文件：`run_20260615T022331Z.jsonl`
- 目前这三套系统都没有继续推进新小时，说明它们现在处于暂停或中断状态。
- 但从已落盘的数据看，本轮运行结果是完整可分析的。

## 二、最新一小时表现

### System 2

- `DIRECTOR_CRM_ALL`
  - 样本：`76`
  - 平均推荐价：`MOP 1279.4`
  - 异常：`9`
- `MARE_ALL`
  - 样本：`76`
  - 平均推荐价：`MOP 1348.2`
  - 异常：`52`
- `SELFACQ_ALL`
  - 样本：`76`
  - 平均推荐价：`MOP 1487.6`
  - 异常：`0`

### System 3

- `DIRECTOR_CRM_ALL_FC`
  - 样本：`76`
  - 平均推荐价：`MOP 1279.4`
  - 异常：`12`
- `MARE_ALL_FC`
  - 样本：`76`
  - 平均推荐价：`MOP 1455.8`
  - 异常：`53`
- `SELFACQ_ALL_FC`
  - 样本：`76`
  - 平均推荐价：`MOP 1490.2`
  - 异常：`0`

### System 1

- 最新运行文件：`run_20260615T022331Z.jsonl`
- `MARE`
  - 样本：`44,308`
  - 平均推荐价：`MOP 1337.9`
  - 异常行：`25,315`
  - ML 命中：`44,308 / 44,308`
- `DIRECTOR`
  - 样本：`44,308`
  - 平均推荐价：`MOP 1286.7`
  - 异常行：`10,513`
- `SELFACQ`
  - 样本：`56,392`
  - 平均推荐价：`MOP 1538.2`
  - 异常行：`0`

## 三、累计 KPI 判断

### 1. 失败率

- 三套系统核心模型的程序性失败率仍然基本为 `0%`
- 这说明九个模型在“能不能跑完”这一层，已经基本稳定

### 2. 异常率

- `SELFACQ`
  - 三套系统几乎都是 `0%`
  - 说明它是当前最稳、最干净的模块
- `DIRECTOR`
  - 正常场景异常率大约在 `11% - 24%`
  - 属于可接受但仍有优化空间
- `MARE`
  - 正常场景异常率大约在 `40% - 57%`
  - 极端场景异常率大约在 `71%` 左右
  - 说明 MARE 的主要矛盾仍然是业务护栏命中率高，而不是程序崩溃

### 3. 收益提升

根据最新双评分卡：

- `S1 SELFACQ normal`：`+33.47%`
- `S1 SELFACQ extreme`：`+33.72%`
- `S2 SELFACQ normal`：`+26.50%`
- `S2 SELFACQ extreme`：`+23.87%`
- `S3 DIRECTOR normal`：`+44.27%`
- `S3 DIRECTOR extreme`：`+25.85%`
- `S1 MARE normal`：`+5.71%`
- `S3 MARE normal`：`+6.78%`
- `S2 MARE normal`：`-1.63%`

这说明：

- `SELFACQ` 仍然是收益质量最稳定的一组
- `Director` 在 `System 3` 上表现最强
- `MARE` 的表现继续分化：
  - `System 3` 版本优于 `System 1`
  - `System 2` 的 MARE 仍然偏弱，甚至在评分卡里出现负提升

## 四、双评分卡结论

根据 `three_model_dual_scorecard_20260617_015732.md`，当前排名前六是：

1. `S1 SELFACQ normal` — `94.80`
2. `S1 SELFACQ extreme` — `94.80`
3. `S2 SELFACQ normal` — `94.00`
4. `S2 SELFACQ extreme` — `94.00`
5. `S3 DIRECTOR normal` — `93.40`
6. `S3 DIRECTOR extreme` — `90.94`

这份排序很稳定，和前几轮分析方向一致。

## 五、三模型收敛判断

如果继续按当前表现收敛，最合理的三模型方向仍然是：

- `SelfACQ`：优先保留，作为最稳的直销 / 获客利润模块
- `Director`：优先参考 `System 3`
- `MARE`：保留，但继续重点优化，不适合按当前版本直接定型

也就是说：

- `SelfACQ` 已经接近“可以定型”
- `Director` 已经达到“可以优先保留”
- `MARE` 还处在“可运行，但还需要继续压异常率和修正业务逻辑”的阶段

## 六、最重要的管理结论

当前九模型最大的变化，不再是“程序稳不稳定”，而是：

- 哪个模块真正能稳定提升收益
- 哪个模块在真实/混合场景下仍然经常撞护栏

所以现在的重点已经不是修启动问题，而是：

- 继续优化 `MARE` 的定价合理性
- 优先保留 `SelfACQ`
- 用 `System 3 Director` 作为 CRM / 直销策略的主参考

## 七、建议

- 短期：
  - 重新启动当前这次替换后的九模型版本
  - 重点盯 `System 2` 和 `System 3` 的 MARE 异常率是否变化
- 中期：
  - 三模型最终定型时，优先锁定 `SelfACQ + Director`
  - `MARE` 作为最后一个重点优化模块继续迭代
- 结论：
  - 当前 KPI 结构仍然非常清晰：
    - `SelfACQ` 最强
    - `Director` 次强
    - `MARE` 最需要继续优化

