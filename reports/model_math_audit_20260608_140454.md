```markdown
# Final Audit Report

## 1. Confirmed Math/Logic Issues
- **F1: Competition Adjustment Divergence Between System 1 and System 2 — Real Logic Flaw**
  - **Severity**: HIGH
  - **Impact**: Different prices under same conditions due to inconsistent logic.

- **F2: `compute_dynamic_ceiling` Missing Return Statement in System 2**
  - **Severity**: CRITICAL
  - **Impact**: Dynamic ceiling feature is non-functional, leading to incorrect pricing.

- **F3: `compute_dynamic_base_price` Uses Different Floor/Ceiling Sources Than DSEC Market Group Logic**
  - **Severity**: HIGH
  - **Impact**: Potential runtime error and inconsistency with business logic.

- **F4: Double Counting of MHA Occupancy Signal**
  - **Severity**: MEDIUM
  - **Impact**: Over-weighting of MHA data could skew final pricing.

- **F5: `get_dsec_demand_signal` Uses Cross-Month Standard Deviation Instead of Within-Month**
  - **Severity**: MEDIUM
  - **Impact**: Systematically underestimates demand signals, reducing model responsiveness.

- **F6: `get_market_group_floor_ceiling` Uses `years_back` Parameter Incorrectly**
  - **Severity**: LOW
  - **Impact**: Slightly outdated bounds due to exclusion of the current year.

- **F7: `get_seasonal_profile` Uses 2023+ Data But `get_market_occupancy` Uses 2023+ for Year=None**
  - **Severity**: LOW
  - **Impact**: Potential bias in seasonal profiles due to inclusion of post-COVID recovery data.

- **F8: `get_latest_market_demand_signal` Uses `hist_end - 1` Logic That May Exclude Relevant Data**
  - **Severity**: LOW
  - **Impact**: May produce misleading signals due to outdated comparisons.

## 2. Expected-Not-a-Bug Warnings
- **M1: `apply_guardrails` Default Values May Be Inappropriate for Luxury Hotels**
  - **Reason**: While the default ceiling may be low for luxury hotels, it is not a bug but rather a design choice that could be documented better.

- **M2: `competition_adjustment` in System 1 Uses Different Weights Than System 2**
  - **Reason**: This is a design inconsistency rather than a critical flaw, as both systems are functioning as intended within their logic.

- **M3: `profit_adjustment` Has Discontinuity at occ=0.95**
  - **Reason**: The discontinuity is minor and unlikely to cause practical issues, thus not a significant concern.

- **M4: `supply_scarcity_adjustment` Has Discontinuity at scarcity_ratio=0.10 and 0.25**
  - **Reason**: While the discontinuity is material, it is a design choice rather than a bug.

- **M5: `get_calibrated_season_multipliers` May Produce Unstable Values With Limited Data**
  - **Reason**: This is a design limitation rather than a bug, as it reflects the statistical fragility of the data used.

- **O1: `get_market_occupancy` and `get_market_adr` Use Different Year Ranges for Year=None**
  - **Reason**: This is an observation regarding documentation consistency rather than a flaw in the logic.

- **O2: `get_latest_market_snapshot` Returns Mass Market Average for 3★+4★**
  - **Reason**: This is acceptable as a proxy but should be documented.

- **O3: `get_dsec_demand_signal` Comment Says "当月入住率 - 长期均值" But Uses Monthly Mean**
  - **Reason**: This is an observation about comment accuracy rather than a logic error.

## 3. Priority Recommendations
1. **Immediate**: Fix the missing return statement in System 2's `compute_dynamic_ceiling()` (F2).
2. **Immediate**: Align competition adjustment logic between System 1 and System 2 (F1).
3. **High Priority**: Implement proper floor/ceiling retrieval in `compute_dynamic_base_price()` (F3).
4. **High Priority**: Fix standard deviation calculation in `get_dsec_demand_signal()` to use within-month variance (F5).
5. **Medium Priority**: Review MHA double-counting and consider reducing one of the two channels (F4).
6. **Medium Priority**: Smooth the discontinuities in `supply_scarcity_adjustment()` (M4).
7. **Low Priority**: Update comments and documentation for consistency (O1, O3).
```